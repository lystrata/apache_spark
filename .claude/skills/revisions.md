Update the Revisions section of the HTML file that was just edited.

Steps:
1. Identify which file was just modified (ask if unclear).
2. Read the file and locate the Revisions section (id: `sec_revisions` for calculators and notes; id: `revisions` for `Document/dev_cluster_math_reference.html`).
3. Determine what changed in the edit — formula, calculation, content, or documentation.
4. Apply the correct update rule:
   - **Formula or calculation change:** add a full entry with today's date, a short title, and a description of what changed and why.
   - **Content change:** add an entry with date, title, and brief description.
   - **Documentation-only change** (comments, labels, styling): note briefly, no full entry required.
   - **Navigation, layout, or styling change only:** do not add a Revisions entry.
5. Insert the new entry at the top of the existing revisions list (most recent first).
6. Show the user the proposed entry before writing it.

Today's date is available in the session context. Use ISO format: YYYY-MM-DD.
