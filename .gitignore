*.swp
.*.swp
.DS_Store
*.bak
*.bak2
*.bak3
*.bak4
Transfer/
Images/
Incoming/

# Cleaned-output archives (also already covered by `Incoming/` above; kept
# explicit so the intent is obvious when grepping .gitignore for Archive/).
phases/phase2/development/Incoming/Archive/

.claudecheckpoints/

# Project-local scratch space (replaces use of system /tmp by Claude Code)
tmp/

# Review staging — derived HTML/PDF renders of project source files; rebuilt as needed.
# Contents are reproducible from the sources in calculators/, correspondence/, phases/, etc.
html_review/
